// Ref Video: https://www.youtube.com/watch?v=gi0AQ78diSA

// A public struct which is imported & used inside a separate file (main_16_2_1_prac.rs).

#[derive(Debug)]
pub struct RandomInfo {
    pub some_int: i8,
    pub some_bool: bool,
}