// Tut-6: Array

/*
Array: can contain only one type of data at a time. 
- Basic array functions
*/

fn main() {
    let arr_1 = [1,2,3,4,5,6];
    println!("1st elem: {}", arr_1[0]);     // get the first elem
    println!("Array length: {}", arr_1.len());  // get the length of the array
}
